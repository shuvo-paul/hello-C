#include <stdio.h>

int main()
{
    int num;
    char ch;
    double d_num;
    float f_num;

    printf("%d\n", sizeof(int));
    printf("size of int: %d\n", sizeof(num));
    printf("size of char: %d\n", sizeof(ch));
    printf("size of d_num: %d\n", sizeof(num));
    printf("size of f_num: %d\n", sizeof(num));

    return 0;
}